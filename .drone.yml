kind: pipeline
type: exec
name: pi-exec

platform:
    os: linux
    arch: arm

steps:
    - name: killnode
      failure: ignore
      commands:
      - killall node

    - name: backend
      detach: true
      commands:
      - npm install
      - npm start
